<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ITP.Views.ToolsPage"
             Title="Money management tools">
    
<ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="20,30">
            <ListView x:Name="ListView1" RowHeight="100" VerticalOptions="CenterAndExpand" ItemTapped="ListView1_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Title}" FontSize="18" TextColor="Black" VerticalTextAlignment="Center"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>